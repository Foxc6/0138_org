<% if current_user.present? %>
<nav class="col-sm-12 col-md-2 sidebar-nav">
	<ul>
		<li <% if controller.controller_name == "welcome" %>class="current"<% end %>><%= link_to 'Home', root_path %></li>
	</ul>
  	<% if current_user.role_id == Role.where(name:'Super Admin').first.id %>
  	<ul class="admin">
  		<li><%= link_to 'Regimens', '/regimens' %></li>
  		<li><%= link_to 'Settings', '/settings' %></li>
  	</ul>
  	<% end %>
  	<div class="acct-links">
  		<%= link_to current_user.full_name, '/account' %>
  		<br>
  		<%= link_to "Sign Out", destroy_user_session_path, data: { confirm: 'Are you sure?' } %>
  	</div>
</nav>
<% end %>



<nav class="sidebar-nav">
  <% if current_user %>
  <form class="search">
    <input name="search" placeholder="Search">
    <span class="magnify"><%= image_tag('magnifying-glass.png') %></span>
  </form>
  <ul>
    <li <% if controller.controller_name == "regimens" %>class="current"<% end %>><%= link_to 'Regimens', regimens_path %></li>
    <li <% if controller.controller_name == "products" %>class="current"<% end %>><%= link_to 'Products', products_path %></li>
    <li <% if controller.controller_name == "product_categories" %>class="current"<% end %>><%= link_to 'Product Categoriess', product_categories_path %></li>
    <li <% if controller.controller_name == "guides" %>class="current"<% end %>><%= link_to 'Guides', guides_path %></li>
  </ul>
  <% end %>
</nav>
